# Molt Tools Hub — Frontend (placeholder)

This directory will contain the Next.js frontend for the Molt Tools Hub. For the MVP v0.1 we provide a static listing renderer that reads from `data/seed.json` and renders entries.

Run (when implemented):

  npm run dev:web

This is currently a placeholder scaffold in the repository. Frontend pages will be added after review of the seed data and README.
